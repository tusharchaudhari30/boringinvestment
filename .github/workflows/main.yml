#
# CI build that assembles artifacts and runs tests.
# If validation is successful this workflow releases from the main dev branch.
#
# - skipping CI: add [skip ci] to the commit message
# - skipping release: add [skip release] to the commit message
#
name: CI

on:
  push:
    branches: ["master"]
    tags-ignore: [v*] # release tags are autogenerated after a successful CI, no need to run CI against them
  pull_request:
    branches: ["**"]
jobs:
  #
  # Main build job
  #
  build:
    runs-on: ubuntu-latest
    if: "! contains(toJSON(github.event.commits.*.message), '[skip ci]')"
    steps:
      - name: 1. Check out code
        uses: actions/checkout@v2 # https://github.com/actions/checkout
        with:
          fetch-depth: "0" # https://github.com/shipkit/shipkit-changelog#fetch-depth-on-ci
      - name: 3. Perform build
        run: gradle build -x test -Dorg.gradle.java.home=$JAVA_HOME_17_X64
      - name: 4. Perform release
        # Release job, only for pushes to the main development branch
        if: github.event_name == 'push'
          && github.ref == 'refs/heads/master'
          && github.repository == 'tusharchaudhari30/boringinvestment'
          && !contains(toJSON(github.event.commits.*.message), '[skip release]')
        run: gradle githubRelease -Dorg.gradle.java.home=$JAVA_HOME_17_X64
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
